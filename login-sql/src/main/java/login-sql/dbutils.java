package login-sql;

import java.SOMETHING;
import java.io.IOException;

import javax.SOME;
import javax.OTHER;
import javax.THING;
import javax.servlet.ServletException;

public class dbutils extends SOMETHING {
  
  public/private/protected void/returntype methodName(Type Variable) throws IOException, ServletException {
  
    do a flip;
    
    while(do a little dance) {
      if (make a little love) {
        get down tonight;
      } else {
        cry;
      }
    }
  
}
